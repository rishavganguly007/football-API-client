{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Tests",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "purpose": ["debug-test"],
            "console": "integratedTerminal",
            "args": [ "-v", "--cov", "--cov-report=xml", "footballAPIClient" ],
            "env": {
                "PYTEST_ADDOPTS": "--cov=./"
            }        
        }

    ]
}